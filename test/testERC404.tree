testERC404
├── when total supply is passed in constructor
│   └── it should mint that much erc20 and erc721 token.
├── when someone send 1 whole erc20 token 
│   └── it transfer that erc20 and that much nft to the receiver.
└── when someone sends fractional erc20 token
    └── Given the balance of erc20 from account 
        └──if the erc20 balance is not whole burn that nft from sender  
        └──if balance is whole mint to the receiver.